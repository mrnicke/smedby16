import {describe,expect,it} from 'vitest';import {blockRegistry} from './blockRegistry';import {validateRedirect} from './redirects';import {claimScheduledJob} from './scheduling';
describe('editor services',()=>{it('registers every supported block with controlled variants',()=>{expect(Object.keys(blockRegistry)).toHaveLength(18);expect(Object.values(blockRegistry).every(item=>item.variants.length>0)).toBe(true);});it('rejects redirect loops and unsafe targets',()=>{expect(validateRedirect('/a/','/b/',[{oldPath:'/b/',newPath:'/a/'}])).toMatch(/loop/);expect(validateRedirect('/a/','https://evil.example',[])).toMatch(/säker/);});it('claims scheduled jobs once when due',()=>{const job={id:'1',status:'pending' as const,executeAt:'2026-01-01T00:00:00Z'};expect(claimScheduledJob(job,new Date('2026-02-01'))?.status).toBe('processing');expect(claimScheduledJob({...job,status:'completed'},new Date('2026-02-01'))).toBeNull();});});
